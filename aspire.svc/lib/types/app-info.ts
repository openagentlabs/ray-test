import type { RegisteredServiceDto } from "@/lib/types/registered-service";

/**
 * Application catalog row used by the registered-services UI (alias of the SQLite registry DTO).
 */
export type AppInfo = RegisteredServiceDto;
