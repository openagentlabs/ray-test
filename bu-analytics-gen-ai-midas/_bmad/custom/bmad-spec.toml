# MIDAS Scenario 1 — spec must preserve PRD/intake traceability

[workflow]

persistent_facts = [
  "Feature slug: must match PRD folder — output to specs/spec-<slug>/ using the same slug the PM chose at intake (Jira ID or kebab-case, unchanged across phases).",
  "file:{project-root}/_bmad-output/scenarios/01-requirements-traceability.md",
  "For Model Lab (scenario 1): create companion traceability.md in the spec folder mapping every PRD/intake requirement to REQ-* and CAP-* IDs. Preserve capability IDs on updates.",
  "Do not invent requirements not present in intake. Gaps become open_questions[] — not silent assumptions.",
  "SPEC Capabilities must be testable and traceable to REQ-* in traceability.md.",
]
