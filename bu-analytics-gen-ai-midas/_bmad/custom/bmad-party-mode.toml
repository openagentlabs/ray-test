# MIDAS — multi-agent roundtable for PRD fidelity + EKS deployability

[workflow]

persistent_facts = [
  "Feature slug: party-review outputs go to planning-artifacts/epics/<slug>/party-reviews/ — use the same slug as PRD and SPEC.",
  "file:{project-root}/_bmad-output/project-context.md",
  "file:{project-root}/_bmad-output/scenarios/01-requirements-traceability.md",
  "MIDAS is AWS-first: private VPC us-east-1, EKS-deployable, S3/Postgres/Redis, no whole-file CSV in RAM, stateless pods, Jenkins pipeline for deploy.",
  "Roundtable must include Winston (architect) for EKS/Helm/memory and John or Mary for PRD/REQ traceability on every Model Lab feature review.",
  "Amelia (dev) must flag implementation risks; Sally (UX) joins when UI stories are discussed.",
  "Save a written synthesis to _bmad-output/planning-artifacts/<slug>/party-reviews/ after each session — do not only chat.",
]

activation_steps_append = [
  "For Scenario 1 Model Lab work: ask which gate applies (backlog review vs pre-dev ST-NNN vs pre-merge). Load PRD, SPEC, traceability, backlog or story, and deploy/ecs-app/helm paths as context for agents.",
]
