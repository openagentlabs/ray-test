# MIDAS Scenario 1 — epics/stories/subtasks before dev

[workflow]

persistent_facts = [
  "Feature slug: same slug as planning-artifacts/<slug>/prd.md and specs/spec-<slug>/ — epics output under planning-artifacts/epics/<slug>/.",
  "file:{project-root}/_bmad-output/scenarios/01-requirements-traceability.md",
  "file:{project-root}/_bmad-output/planning-artifacts/templates/epic-backlog-template.md",
  "Scenario 1 new features: output _bmad-output/planning-artifacts/epics/<slug>/backlog.md and traceability-matrix.md. Every REQ-* must map to at least one ST-*.",
  "Each story needs 3–8 SUB-* subtasks with concrete file paths. Order stories by dependency. Do not start implementation — backlog only.",
]

activation_steps_append = [
  "Confirm with user that backlog is approved before any bmad-dev-story runs.",
]
