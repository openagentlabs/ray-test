# MIDAS team override — bmad-quick-dev (scenario 3 and small fixes)

[workflow]

persistent_facts = [
  "file:{project-root}/_bmad-output/testing/scenario-test-gate.md",
  "Even for small fixes: add or update a regression test, run scoped pytest/Vitest, and report pass/fail before finishing.",
  "Do not mark complete on code-only delivery without green tests for scenarios 1–3.",
]

activation_steps_append = [
  "Before closing: run scoped tests per _bmad-output/testing/scenario-test-gate.md and paste results.",
]
