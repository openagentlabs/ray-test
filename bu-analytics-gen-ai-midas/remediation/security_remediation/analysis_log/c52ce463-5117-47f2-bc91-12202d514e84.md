# Analysis log — OBV0259

- **analysis_id:** `c52ce463-5117-47f2-bc91-12202d514e84`
- **Fortify priority:** Low
- **Category:** Weak Cryptographic Hash
- **Sink:** `` line 
- **Source:** `` line 

## Root cause
Weak Cryptographic Hash in unknown file (path truncated) (path truncated). The concrete instance is backend/app/api/routes.py: hashlib.md5() is called at lines 5484-5485, 5518-5519, and 6706-6707 to compute a 'display_fingerprint' and 'stored_fingerprint' / 'download_fingerprint' for a dataset. MD5 is a cryptographically broken hash function vulnerable to collision attacks. If these fingerprints are used for integrity verification (ensuring a downloaded dataset has not been tampered with), MD5 provides no real security guarantee.

## Remediation plan
1) backend/app/api/routes.py lines 5484, 5518, 6706: replace hashlib.md5() with hashlib.sha256() — identical API, stronger security.
2) If the fingerprint is only used for display/deduplication (not security-critical integrity), document this with a comment; otherwise use SHA-256.
3) Update any database schema column that stores MD5 fingerprints to accommodate SHA-256 hex digests (64 chars vs 32).
4) Run: grep -rn 'hashlib.md5\|hashlib.sha1' backend/ to find all occurrences and apply the same fix.
5) Add bandit rule B303 (MD5/SHA1) to CI.

## Validation
grep -rn 'hashlib.md5\|hashlib.sha1' backend/ returns empty after remediation; bandit B303 passes; existing fingerprint columns updated to SHA-256 length in DB migration.

## Acceptance criteria
1) No hashlib.md5() calls remain in backend/ for security-relevant fingerprinting.
2) hashlib.sha256() used for all fingerprints.
3) bandit B303 passes in CI.
4) Database columns updated to store SHA-256 digests.
5) Fortify Weak Cryptographic Hash finding cleared on next rescan.

## Disposition
- **resolution_owner:** AI_AGENT
- **owner (team):** Software
- **complexity:** LOW
- **human_fix_hours:** 2
- **cursor_fix_hours:** 1
- **hybrid_fix_hours:** 3

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._