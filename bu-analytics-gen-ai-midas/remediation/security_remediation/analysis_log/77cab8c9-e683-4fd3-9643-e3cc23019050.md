# Analysis log — OBV0220

- **analysis_id:** `77cab8c9-e683-4fd3-9643-e3cc23019050`
- **Fortify priority:** Critical
- **Category:** Insecure Transport
- **Sink:** `` line 
- **Source:** `` line 

## Root cause
Insecure Transport finding with no file path resolved by the scanner (file_found=false, sink_file empty). Based on the explanation and the codebase, the most likely sources are: (1) GRAPHRAG_SERVICE_URL=http://graphrag:8001 hardcoded in docker-compose.yml line 96 and JENKINS_DEPLOYMENT_10April.md - plain HTTP used for GraphRAG inter-service communication; (2) frontend/src/services files making HTTP (not HTTPS) calls to internal service endpoints. In the MIDAS VPC all service-to-service traffic should use HTTPS or TLS-terminated service mesh connections. Plain HTTP is acceptable only within the Docker Compose local dev network.

## Remediation plan
1) docker-compose.yml and docker-compose.aws.yml: GRAPHRAG_SERVICE_URL is correctly set to http:// for local Docker networking only - document explicitly with a comment that this is local-only and must never appear in ECS/EKS task definitions.
2) In the ECS/EKS task definition and Helm values, ensure GRAPHRAG_SERVICE_URL uses https:// or the Kubernetes service DNS with TLS. Verify deploy/ecs-app/ Terraform and Helm chart values files do not set any http:// service URLs for production.
3) Scan frontend/src/services/ for hardcoded http:// base URLs using grep -r 'http://' frontend/src/services/ - replace any found with environment-variable-driven BASE_URL that is always https:// in non-local environments.
4) Add a Checkov rule (CKV_DOCKER_2 or custom) to CI to flag http:// in env vars of production compose or task-definition files.

## Validation
grep -r 'http://' deploy/ecs-app/ returns no plain HTTP service URLs; frontend production build uses https:// base URLs (check build output with VITE_API_BASE_URL); Fortify Insecure Transport findings clear on rescan for non-localhost addresses.

## Acceptance criteria
1) No http:// URLs appear in deploy/ecs-app/ Terraform or Helm values files for service-to-service endpoints.
2) frontend production bundle uses only https:// for API base URLs.
3) docker-compose.yml GRAPHRAG_SERVICE_URL http:// is commented as local-dev-only.
4) CI Checkov scan passes with no insecure transport violations in deploy/.
5) Fortify Insecure Transport category cleared for this finding on next rescan.

## Disposition
- **resolution_owner:** AI_AGENT
- **owner (team):** DevOps
- **complexity:** MID
- **human_fix_hours:** 4
- **cursor_fix_hours:** 2
- **hybrid_fix_hours:** 5

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._