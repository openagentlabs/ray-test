# Analysis log — OBV0208

- **analysis_id:** `998b7be7-21f2-4d87-8f96-54d540855e2b`
- **Fortify priority:** Critical
- **Category:** Cross-Site Scripting: DOM
- **Sink:** `` line 
- **Source:** `docs/html_diagram/` line 

## Root cause
A second distinct Fortify finding in docs/html_diagram/ (same directory as OBV0207). The concrete sinks are in the classic and layered sub-diagrams: docs/html_diagram/final/classic/index.html line 893 and future/classic/index.html line 893: tip.innerHTML = html where html is built from el.dataset.ttTitle and el.dataset.ttComponents via template literals - not all paths pass through escapeHtml. docs/html_diagram/final/layered/index.html and future/layered/index.html lines 1112-1179: tt.innerHTML = h and dpDesc.innerHTML = fmtText(desc) where fmtText calls escapeHtml then appends raw <br/> tags. The risk: an attacker-supplied URL hash (#component-id) reaches document.querySelector(hash) which selects an SVG element whose data-* attributes are then injected into innerHTML.

## Remediation plan
1) In classic/index.html (both final/ and future/): review the html template literal on lines 889-892 - ensure every el.dataset.* value passes through escapeHtml() before insertion into the string.
2) In layered/index.html: the fmtText() function calls escapeHtml() before inserting <br/> - verify escapeHtml is defined and covers all five HTML special characters (<, >, &, ", ').
3) Add a document.querySelector(hash) guard: before using a URL hash to select elements, validate it matches /^#[a-zA-Z0-9_-]+$/ to prevent CSS injection via the selector.
4) Add a Content-Security-Policy header to the docs serving origin restricting script-src to self.
5) Exclude docs/html_diagram/ from the production ECR image if these pages are developer-only artifacts.

## Validation
Inject a crafted hash (#<img src=x onerror=alert(1)>) in the URL and verify the browser does not execute the onerror handler; verify escapeHtml output for all five special characters; CSP header present in HTTP response.

## Acceptance criteria
1) All dataset.* values in the classic and layered diagram innerHTML template literals pass through verified escapeHtml.
2) URL hash selector is validated against an allowlist regex before use.
3) CSP header present on the docs serving origin.
4) Fortify DOM XSS category cleared for docs/html_diagram/ on next rescan.
5) Manual payload test confirms no XSS execution.

## Disposition
- **resolution_owner:** AI_AGENT
- **owner (team):** Software
- **complexity:** MID
- **human_fix_hours:** 4
- **cursor_fix_hours:** 2
- **hybrid_fix_hours:** 5

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._