# Analysis log — OBV0096

- **analysis_id:** `eb756c14-9565-497f-ad75-8b9b54a08e91`
- **Fortify priority:** High
- **Category:** Credential Management: Hardcoded API Credentials
- **Sink:** `backend/app/core/session/path_policy.py` line 2
- **Source:** `` line 

## Root cause
backend/app/services/auth_service.py line 20: SECRET_KEY = 'your-secret-key-here-change-in-production'. This HS256 JWT signing key is hardcoded as a placeholder string never replaced with a real secret. Any JWT signed with this key can be forged by anyone with repository access, enabling complete authentication bypass.

## Remediation plan
1) backend/app/services/auth_service.py line 20: replace with SECRET_KEY = os.environ['JWT_SECRET_KEY'] with no default fallback.
2) Provision a 256-bit random secret via openssl rand -hex 32 and store in AWS Secrets Manager.
3) Inject JWT_SECRET_KEY via ECS task definition environment sourced from Secrets Manager.
4) Add backend/.env.example entry: JWT_SECRET_KEY=<generate-with-openssl-rand-hex-32>.
5) After deploying, all existing sessions signed with the old key are invalid — ensure frontend handles 401 by redirecting to login.

## Validation
grep 'your-secret-key' backend/app/services/auth_service.py returns empty; backend startup fails with KeyError if JWT_SECRET_KEY unset; old-key-signed JWT rejected after rotation.

## Acceptance criteria
1) No hardcoded SECRET_KEY in auth_service.py.
2) App fails to start if JWT_SECRET_KEY env var is unset.
3) JWT_SECRET_KEY stored in Secrets Manager and injected via ECS.
4) Existing sessions invalidated.
5) Fortify finding cleared on next rescan.

## Disposition
- **resolution_owner:** H_REQ
- **owner (team):** Software
- **complexity:** MID
- **human_fix_hours:** 4
- **cursor_fix_hours:** 2
- **hybrid_fix_hours:** 7

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._