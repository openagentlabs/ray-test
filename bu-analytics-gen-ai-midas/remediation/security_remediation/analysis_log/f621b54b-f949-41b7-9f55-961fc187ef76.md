# Analysis log — OBV0091

- **analysis_id:** `f621b54b-f949-41b7-9f55-961fc187ef76`
- **Fortify priority:** High
- **Category:** Command Injection
- **Sink:** `deploy/scripts/util/aws-ssm-port-forward-` line 
- **Source:** `deploy/scripts/uti` line 

## Root cause
deploy/scripts/util/aws-ssm-port-forward-all.py and aws-ssm-port-forward-midas-and-ai-gateway.py use subprocess.run() and subprocess.Popen() with list-form arguments for all AWS CLI and SSM calls. No shell=True is used. Fortify flags because argparse-provided values (--instance-id, --local-port, --region, --profile) flow into subprocess call lists without explicit allowlist validation. The actual risk is low since list-form subprocess calls prevent shell metacharacter injection, but an adversary with control over CLI invocation could pass unusual values for --instance-id or --local-port.

## Remediation plan
1) deploy/scripts/util/aws-ssm-port-forward-all.py and aws-ssm-port-forward-midas-and-ai-gateway.py: add argparse type validators for key args: instance_id must match /^i-[0-9a-f]{17}$/; port args must be int in range 1024-65535; region must match /^[a-z]{2}-[a-z]+-[0-9]$/; profile must match /^[a-zA-Z0-9._-]+$/.
2) Confirm no shell=True calls remain (already confirmed: none present).
3) Add a comment in each file noting the safe subprocess pattern.

## Validation
python3 deploy/scripts/util/aws-ssm-port-forward-all.py --instance-id 'bad;id' raises argparse error before subprocess; grep 'shell=True' deploy/scripts/util/*.py returns empty.

## Acceptance criteria
1) Instance ID, port, region, and profile args have allowlist validators in argparse.
2) grep 'shell=True' returns empty for all scripts in deploy/scripts/util/.
3) Fortify Command Injection category cleared on next rescan.
4) Scripts function correctly with valid inputs after validation added.

## Disposition
- **resolution_owner:** AI_AGENT
- **owner (team):** DevOps
- **complexity:** LOW
- **human_fix_hours:** 2
- **cursor_fix_hours:** 1
- **hybrid_fix_hours:** 3

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._