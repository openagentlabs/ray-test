# Analysis log — OBV0255

- **analysis_id:** `e69bb5ad-25bc-4c98-9498-8d4ac109fbd3`
- **Fortify priority:** Medium
- **Category:** System Information Leak: External
- **Sink:** `frontend/src/pages/ModelBuilder.tsx` line 2203
- **Source:** `frontend/src/pages` line 

## Root cause
frontend/src/pages/ModelBuilder.tsx line 2203: sessionStorage.setItem('dataset_classification_error', JSON.stringify({error: true, message: 'AI classification encountered an error. Using default Others type.', timestamp: ..., details: errMsg})). The details field contains errMsg which is the full error message string from a classificationError (an Error object). Detailed error messages stored in sessionStorage and potentially rendered to the user can leak internal system information (stack traces, API endpoint names, internal service responses) to an attacker who has XSS access or can read sessionStorage.

## Remediation plan
1) frontend/src/pages/ModelBuilder.tsx line 2203: sanitize errMsg before storing — strip anything beyond a safe generic message: const safeMsg = errMsg.length > 200 ? errMsg.substring(0, 200) : errMsg.
2) Better: store only a sanitized user-facing message and log the full technical error to a backend logging endpoint (not sessionStorage).
3) Remove the details field from sessionStorage entirely and replace with a generic error code.
4) Any place errMsg is rendered in the UI: ensure it does not render raw Error.message that could include internal URLs, stack traces, or secrets.

## Validation
Manual test with a failing classification API call — confirm sessionStorage dataset_classification_error.details contains only a truncated, non-technical message; no internal API URLs or stack trace fragments visible.

## Acceptance criteria
1) details field in sessionStorage contains only a safe, truncated user-facing message.
2) Full technical error is logged to a backend endpoint, not stored client-side.
3) No internal system information visible to users in error messages.
4) Fortify finding cleared on next rescan.

## Disposition
- **resolution_owner:** AI_AGENT
- **owner (team):** Software
- **complexity:** LOW
- **human_fix_hours:** 2
- **cursor_fix_hours:** 1
- **hybrid_fix_hours:** 3

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._