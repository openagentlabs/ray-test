# Analysis log — OBV0077

- **analysis_id:** `577ef72d-e676-4c14-a7e6-368a5b7c37bf`
- **Fortify priority:** High
- **Category:** Command Injection
- **Sink:** `` line 
- **Source:** `` line 

## Root cause
.cursor/scripts/aww-cert-generate-injector.py uses subprocess.run() with list-form arguments (not shell=True) throughout, passing CLI argparse-provided values (domain names, certificate CN, profile names, region) as individual list items. This is the safe subprocess pattern — Fortify flags it because argparse args flow into subprocess calls without additional whitelist validation. Since shell=True is never used, OS shell metacharacter injection is not possible; however, a malicious --domain value (e.g. containing path traversal) could affect the openssl command output path or ACM certificate metadata.

## Remediation plan
1) .cursor/scripts/aww-cert-generate-injector.py: for each argparse argument that is passed to subprocess (--domain, --profile, --region, --cn), add input validation — domain names: allow only [a-zA-Z0-9.-]; region: allow only [a-z0-9-]; profile: allow only [a-zA-Z0-9._-].
2) Add a guard: if shlex.quote(value) != value: raise ValueError(f'Invalid characters in argument: {value}').
3) This script runs only on developer laptops (never in CI or production), so the actual risk is low, but the validation hardening is still warranted.
4) Add a comment noting that shell=True is intentionally not used.

## Validation
python3 .cursor/scripts/aww-cert-generate-injector.py --domain '<img src=x>' raises ValueError before any subprocess call; grep 'shell=True' .cursor/scripts/aww-cert-generate-injector.py returns empty.

## Acceptance criteria
1) Input validation (allowlist regex) applied to all CLI args that flow into subprocess calls.
2) grep 'shell=True' returns empty for the file.
3) Fortify Command Injection category cleared for .cursor/scripts/aww-cert-generate-injector.py on next rescan.
4) Existing functionality works correctly with valid inputs after validation added.

## Disposition
- **resolution_owner:** AI_AGENT
- **owner (team):** Software
- **complexity:** LOW
- **human_fix_hours:** 2
- **cursor_fix_hours:** 1
- **hybrid_fix_hours:** 3

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._