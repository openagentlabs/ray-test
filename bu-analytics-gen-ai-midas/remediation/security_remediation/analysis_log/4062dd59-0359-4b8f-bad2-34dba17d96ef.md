# Analysis log — OBV0095

- **analysis_id:** `4062dd59-0359-4b8f-bad2-34dba17d96ef`
- **Fortify priority:** Critical
- **Category:** Credential Management: Hardcoded API Credentials
- **Sink:** `frontend/token.txt` line 1
- **Source:** `` line 

## Root cause
frontend/token.txt line 1 contains a live UTF-16 encoded JWT bearer token (three-part eyJ… structure visible in the null-byte-interleaved content) committed directly to the git repository. Any developer or CI system with repo access can read and replay this token. The file is not in .gitignore so it has been committed and is in git history.

## Remediation plan
1) Immediately revoke the token via the issuing identity provider (the JWT sub field shows testuser — invalidate all sessions for this identity).
2) Delete frontend/token.txt from the repo and add it to .gitignore.
3) Purge from git history: git filter-repo --path frontend/token.txt --invert-paths (or BFG Repo-Cleaner).
4) Force-push the cleaned history and rotate any dependent secrets.
5) Replace runtime token acquisition with environment-variable injection (e.g. TOKEN env var set by the deployment pipeline, never written to disk).

## Validation
git log --all --full-history -- frontend/token.txt returns no commits after history purge; gitleaks scan (gitleaks detect --source .) reports zero findings for this path; runtime auth still works with env-var-supplied token.

## Acceptance criteria
1) frontend/token.txt does not exist on any branch or tag in the remote repo after purge.
2) frontend/token.txt is listed in .gitignore.
3) The original token is revoked and any session using it is invalid.
4) gitleaks CI check passes on default branch.
5) App authentication works without the file present.

## Disposition
- **resolution_owner:** H_REQ
- **owner (team):** Software
- **complexity:** HIGH
- **human_fix_hours:** 8
- **cursor_fix_hours:** 4
- **hybrid_fix_hours:** 10

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._