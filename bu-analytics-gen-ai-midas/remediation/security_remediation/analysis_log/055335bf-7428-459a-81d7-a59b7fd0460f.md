# Analysis log — OBV0012

- **analysis_id:** `055335bf-7428-459a-81d7-a59b7fd0460f`
- **Fortify priority:** High
- **Category:** AWS Terraform Misconfiguration: Improper EKS Network Access Control
- **Sink:** `deploy/ai_gateway/terraform/modules-midas/eks_cluster.tf` line 15
- **Source:** `` line 

## Root cause
deploy/ai_gateway/terraform/modules-midas/eks_cluster.tf line 15: the EKS cluster vpc_config block sets endpoint_public_access = true alongside endpoint_private_access = true. This exposes the Kubernetes API server endpoint to the public internet. The MIDAS architecture rule mandates private-by-default — no public endpoints. Any IP can attempt to authenticate against the API endpoint, expanding the attack surface.

## Remediation plan
1) deploy/ai_gateway/terraform/modules-midas/eks_cluster.tf: change endpoint_public_access = false.
2) Confirm all CI/CD access to the cluster (Jenkins) routes through the VPC or Transit Gateway — it should already use the private endpoint via the jumpbox/EKS tunnel scripts.
3) If a public endpoint is needed for a temporary reason, document the exception in an ADR and restrict public_access_cidrs to the corporate IP range only.
4) Add Checkov CKV_AWS_58 (EKS private endpoint) to CI.

## Validation
terraform plan shows endpoint_public_access = false; aws eks describe-cluster output shows publicAccessEnabled: false; Checkov CKV_AWS_58 passes.

## Acceptance criteria
1) endpoint_public_access = false in the EKS cluster Terraform resource.
2) Jenkins pipeline can still deploy via private endpoint.
3) Checkov CKV_AWS_58 passes.
4) aws eks describe-cluster confirms publicAccessEnabled: false.
5) Fortify finding cleared on next rescan.

## Disposition
- **resolution_owner:** H_REQ
- **owner (team):** DevOps
- **complexity:** MID
- **human_fix_hours:** 4
- **cursor_fix_hours:** 2
- **hybrid_fix_hours:** 7

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._