# Analysis log — OBV0015

- **analysis_id:** `871f1247-bb2d-40ab-8d62-c9705bea72d0`
- **Fortify priority:** Medium
- **Category:** AWS Terraform Misconfiguration: Insecure EKS Storage
- **Sink:** `deploy/ecs-app/modules/eks/main.tf` line 112
- **Source:** `` line 

## Root cause
deploy/ecs-app/modules/eks/main.tf line 112: the aws_eks_cluster resource has an encryption_config block that encrypts 'secrets' resources — this is the correct pattern. Fortify may flag that the encryption_config only covers 'secrets' and not all resources, or that the KMS key policy needs review. The finding likely refers to missing encryption for EKS persistent volumes (EBS CSI driver default storage class not enforcing encryption).

## Remediation plan
1) Confirm the EKS default StorageClass used by the EBS CSI driver has encrypted: 'true' in the parameters. Add or update the StorageClass resource in the Helm chart or Terraform.
2) In deploy/ecs-app/modules/eks/main.tf: verify encryption_config resources includes 'secrets' (already present per code read).
3) Add a Kubernetes StorageClass with encrypted: 'true' and kmsKeyId set to the EKS KMS key.
4) Add Checkov CKV_AWS_58 (EKS secrets encryption) to CI.

## Validation
kubectl get storageclass shows encrypted: 'true'; terraform plan shows encryption_config resources = ['secrets']; Checkov CKV_AWS_58 passes.

## Acceptance criteria
1) EKS default StorageClass enforces EBS encryption.
2) encryption_config covers secrets in EKS cluster.
3) Checkov CKV_AWS_58 passes.
4) Fortify finding cleared on next rescan.

## Disposition
- **resolution_owner:** AI_AGENT
- **owner (team):** DevOps
- **complexity:** MID
- **human_fix_hours:** 3
- **cursor_fix_hours:** 2
- **hybrid_fix_hours:** 4

---
_Generated by isg-row-analyzer (agent-driven per-row analysis)._