---
description: REPLACE_ME — one sentence for the rule picker; what this rule enforces or teaches.
alwaysApply: false
# globs: optional YAML list — uncomment and set file patterns when alwaysApply is false.
# Example:
# globs:
#   - "**/*.py"
---

# REPLACE_ME — rule title (human-readable)

**What this file is:** REPLACE_ME — scope and audience from the AI agent’s perspective.

**When to use it:** REPLACE_ME — triggers (tasks, paths, user requests) that should load this rule.

**How to use it:** REPLACE_ME — ordered steps or constraints; link related rules instead of duplicating constants.

---

## Rules

<!-- Add one actionable rule per bullet or numbered item below. Keep each rule concrete and verifiable. -->
